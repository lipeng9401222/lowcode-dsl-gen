# 字段类型约束表

> ⚠️ **生成 workflow 前必读！** YAML 中数字和字符串的写法不同会导致解析类型不同，这是最常见的错误来源。

## 核心规则

1. **字符串类型字段必须加引号**：`tag: '20'`（字符串 `"20"`），不加引号的 `tag: 20` 会被解析为整数
2. **整数类型字段不要加引号**：`multitransactormode: 10`（整数），加引号的 `multitransactormode: '10'` 会被解析为字符串

## 完整字段类型速查表

### 必须为 **整数** 的字段

| 字段 | 所属节点 | 值域示例 | 常见错误 |
|------|---------|---------|---------|
| `activitytype` | activity | `10` `20` `30` `100` | ❌ 写成字符串 `'30'` |
| `splittype` | activity | `30` | ❌ 写成字符串 `'30'` |
| `jointype` | activity | `30` | ❌ 写成字符串 `'30'` |
| `multitransactormode` | activity | `10` | ❌ 写成字符串 `'10'` |
| `isallowaddattachfile` | activity | `20` | ❌ 写成字符串 `'20'` |
| `timelimitenable` | activity | `20` | ❌ 写成字符串 `'20'` |
| `earlywarning_enable` | activity | `20` | ❌ 写成字符串 `'20'` |
| `is_passwhennotransactor` | activity | `20` | ❌ 写成字符串 `'20'` |
| `operationtype` | workflowActivityOperation | `10` `30` | ❌ 写成字符串 `'10'` |
| `is_sendtomessagecenter` | transition | `10` | ❌ 写成字符串 `'10'` |
| `priority` | transition | `60` | ❌ 写成字符串 `'60'` |
| `type` | transition | `10` | ❌ 写成字符串 `'10'` |
| `revokeoption` | workflowProcessVersion | `10` `20` `30` `40` | ❌ 写成字符串 |
| `noparticipatoroption` | workflowProcessVersion | `10` `20` `30` | ❌ 写成字符串 |
| `revokeremindoption` | workflowProcessVersion | `10` `20` | ❌ 写成字符串 |
| `belongto` | workflowConfig | `22` | ❌ 写成字符串 |

### 必须为 **字符串** 的字段

| 字段 | 所属节点 | 值域示例 | 常见错误 |
|------|---------|---------|---------|
| `iconx` | activity | `'-159'` `'26.5'` | ❌ 写成数字 `-159` |
| `icony` | activity | `'123'` `'-20'` | ❌ 写成数字 `123` |
| `direction` | workflowProcessVersion | `'0'` `'90'` | ❌ 写成数字 `90` |
| `tag` | workflowProcess | `'10'` `'20'` | ❌ 写成数字 `20` |
| `configvalue` | workflowConfig | `'1'` | ❌ 写成数字 `1` |
| `operationvisiablecase` | workflowActivityOperation | `'10'` `'1020'` | ❌ 写成数字 `1020` |
| `backtargetscope` | workflowActivityOperation | `'1'` `'2'` | ❌ 写成数字 `1` |
| `nopasshandlevalue` | activity | `'10'` `'20'` | ❌ 写成数字 `10` |
| `isshowlinegraph` | workflowProcessVersion | `'Normal'` `'Orthogonal'` | ❌ 拼写错误 |
| `isshownodesimple` | workflowProcessVersion | `'details'` `'simple'` | ❌ 拼写错误 |
| `multitransctormode` | workflowActivityOperation | `'OR'` `'AND'` `'OrAndRead'` | ❌ 拼写错误 |
| `encrypt` | workflowMethodParameter | `'0'` `'1'` | ❌ 写成数字 |

## 校验脚本自动检测

`scripts/validate_yml.py` 已内置以上所有类型校验：
- `iconx/icony` 非字符串 → **warn**
- `multitransactormode` 等为字符串 → **warn**
- `direction/tag` 为整数 → **warn**
- `workflowProcessVersion` 工作流字段值域 → **warn**
- 检测到 `tableid` / `fromMisTableId` / `fromFieldId` / `isDefault` 等驼峰/已废弃字段 → **warn** 引导迁移到 spec v2
